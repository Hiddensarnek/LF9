services:
  verandb:
    build: https://github.com/Hiddensarnek/LF9-AlexPaul.git
    image: verandb
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - verandb_data:/app

volumes:
  verandb_data:
